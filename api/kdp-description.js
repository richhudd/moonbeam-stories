const {verifyMoonbeamUser}=require('../_credits');
module.exports=async function handler(req,res){
 res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type','application/json; charset=utf-8');
 if(req.method!=='POST')return res.status(405).json({error:'POST only'});
 try{
  const user=await verifyMoonbeamUser(req),developerEmail=String(process.env.MOONBEAM_DEVELOPER_EMAIL||'').trim().toLowerCase();
  if(!developerEmail||String(user.email||'').trim().toLowerCase()!==developerEmail)return res.status(403).json({error:'Developer access only.'});
  const apiKey=String(process.env.OPENAI_API_KEY||'').trim().replace(/^[\'\"]|[\'\"]$/g,'');if(!apiKey)return res.status(500).json({error:'OPENAI_API_KEY is not configured in Vercel.'});
  const body=typeof req.body==='string'?JSON.parse(req.body):(req.body||{}),story=body.story||{},author=String(body.author||'').trim(),series=String(body.series||'').trim();
  const parts=[story.opening,...(Array.isArray(story.pages)?story.pages.map(p=>p?.text||''):[]),story.closing].filter(Boolean),full=parts.join('\n\n');if(!story.title||!full)return res.status(400).json({error:'The finished story is incomplete.'});
  const prompt=`Write the Amazon KDP product description for this finished children's story.\n\nTITLE: ${story.title}\nAUTHOR: ${author}\nSERIES: ${series}\n\nFINISHED STORY:\n${full}\n\nRequirements:\n- Return ONLY the description as plain text, with no heading, labels, markdown, HTML, bullet points or quotation marks.\n- Aim for 100-150 words.\n- This is enticing sales copy, not a full synopsis.\n- Describe only characters, settings and events that actually occur in the finished story. Never invent details or selling points.\n- Introduce the central adventure and its hook, but do not reveal the ending or resolution.\n- Natural British English.\n- Avoid generic AI/marketing phrases such as “embark on a magical journey”, “heartwarming tale”, “perfect for”, “young readers will love”, “packed with”, or “join X as”.\n- Do not mention AI, prompts, generation, personalisation, Moonbeam Stories, or how the book was made.\n- You may naturally identify it as part of ${series} if useful, but do not force the series name into the copy.\n- Keep the tone specific to this particular story rather than using a reusable template.`;
  const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5.6-luna',input:prompt,max_output_tokens:500})});
  const raw=await r.text();let data={};try{data=JSON.parse(raw)}catch{}if(!r.ok){const e=data?.error;throw new Error(typeof e==='string'?e:(e?.message||`OpenAI returned HTTP ${r.status}`))}
  let out=typeof data.output_text==='string'?data.output_text:'';if(!out&&Array.isArray(data.output))for(const item of data.output)for(const part of (item.content||[]))if(typeof part.text==='string')out+=part.text;out=out.trim().replace(/^[\'\"]|[\'\"]$/g,'').trim();if(!out)throw new Error('The KDP description came back empty.');return res.status(200).json({description:out});
 }catch(e){console.error('KDP description error',e);return res.status(e.status||500).json({error:e.message||'The KDP description could not be generated.'})}
};
