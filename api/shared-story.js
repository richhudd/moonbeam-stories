const {SUPABASE_URL,adminHeaders}=require('../_usage');
const {getShareByToken,getSavedStory,jsonFetch}=require('../_shares');
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'GET only'});try{const token=String(req.query?.token||'').trim(),share=await getShareByToken(token);if(!share)return res.status(404).json({error:'This story link is unavailable or has been revoked.'});const story=await getSavedStory(share.saved_story_id);if(!story)return res.status(404).json({error:'This story is unavailable.'});let child={name:'',age:7,interests:'',dislikes:''};if(story.child_id){const rows=await jsonFetch(`${SUPABASE_URL}/rest/v1/child_profiles?id=eq.${encodeURIComponent(story.child_id)}&select=name,age,interests,dislikes`,{headers:adminHeaders()});if(rows?.[0])child=rows[0]}
 if(!share.opened_at)fetch(`${SUPABASE_URL}/rest/v1/story_shares?id=eq.${encodeURIComponent(share.id)}`,{method:'PATCH',headers:adminHeaders({'Content-Type':'application/json',Prefer:'return=minimal'}),body:JSON.stringify({opened_at:new Date().toISOString()})}).catch(()=>{});
 const pageCount=Array.isArray(story.saved_assets?.pages)?story.saved_assets.pages.length:0;return res.status(200).json({title:story.title,senderName:share.sender_name,language:story.language,child,story:{title:story.title,opening:story.opening||'',character_bible:story.character_bible||'',pages:Array.isArray(story.pages)?story.pages:[],closing:story.closing||''},savedAssets:{cover:'share:cover',pages:Array.from({length:pageCount},(_,i)=>`share:${i}`)}})}catch(e){console.error('shared-story',e);return res.status(e.status||500).json({error:String(e.message||e)})}};
