const {SUPABASE_URL,adminHeaders}=require('../_usage');const {getShareByToken,getSavedStory}=require('../_shares');
module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).send('GET only');try{const token=String(req.query?.token||''),kind=String(req.query?.kind||''),share=await getShareByToken(token);if(!share)return res.status(404).send('Unavailable');const story=await getSavedStory(share.saved_story_id);if(!story)return res.status(404).send('Unavailable');let path;if(kind==='cover')path=story.saved_assets?.cover;else if(/^\d+$/.test(kind))path=story.saved_assets?.pages?.[Number(kind)];if(!path)return res.status(404).send('Image unavailable');const r=await fetch(`${SUPABASE_URL}/storage/v1/object/${String(path).split('/').map(encodeURIComponent).join('/')}`,{headers:adminHeaders()});if(!r.ok)return res.status(404).send('Image unavailable');const bytes=Buffer.from(await r.arrayBuffer());res.setHeader('Content-Type',r.headers.get('content-type')||'image/webp');res.setHeader('Cache-Control','private, max-age=3600');return res.status(200).send(bytes)}catch(e){console.error('shared-asset',e);return res.status(500).send('Image unavailable')}};
